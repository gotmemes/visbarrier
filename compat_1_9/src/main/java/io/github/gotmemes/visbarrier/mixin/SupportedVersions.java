package io.github.gotmemes.visbarrier.mixin;

// Stub — root copy (with correct @Retention) wins at jar merge.
// Exists only so compat/v1_9 mixin classes can compile against this FQN.
public @interface SupportedVersions {
    String value();
}
